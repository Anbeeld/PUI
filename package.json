{
  "name": "pui",
  "version": "1.0.1",
  "private": true,
  "description": "Opinionated batteries-included composition profile for vanilla Pi. Setup-time scripts and repository metadata only.",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:verbose": "node --test --test-reporter spec tests/*.test.js"
  }
}
